// Shared build settings for local development.
//
// Defaults:
// If you don't provide a value in `Secrets.xcconfig`, sign-in will show "not configured".
ONEDRIVE_CLIENT_ID = YOUR_ONEDRIVE_CLIENT_ID

// Optional overrides (gitignored):
// Copy `Secrets.xcconfig.example` -> `Secrets.xcconfig` and fill in values.
#include? "GPhotoPaper/Secrets.xcconfig"
#include? "Secrets.xcconfig"
